# Sol Vibe - Solana 二元预测市场设计文档

## 概述

Sol Vibe 是一个基于 Sonic SVM 的二元预测市场，支持两种市场类型：
1. **价格预测市场** - 预测加密货币价格（Pyth 自动结算）
2. **通用事件市场** - 预测任意事件（创建者裁定）

用户可以使用任意 SPL 代币作为投注货币。

### 核心特性

- **市场类型**: 价格预测 + 通用事件
- **投注代币**: 创建者指定任意 SPL 代币（如 TRUMP、USDC）
- **定价机制**: CPMM（通过 Sega 实现）
- **Oracle**: Pyth Network（仅价格市场）
- **市场创建**: 开放（任何人可创建）
- **结算方式**:
  - 价格市场：Pyth 自动判定
  - 事件市场：创建者裁定
- **技术栈**: Anchor + Next.js + React

---

## 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                      Frontend (Next.js)                 │
│      市场浏览 / 创建市场 / 查看持仓 / 裁定 / 赎回        │
└───────────────┬─────────────────────────────────────────┘
                │
                ▼
┌───────────────────────┐   ┌─────────────────────────────┐
│    Market Factory     │   │        Settlement           │
│    (Anchor Program)   │   │     (Anchor Program)        │
│                       │   │                             │
│ • create_price_market │   │ • settle_price_market       │
│ • create_event_market │   │   (Pyth 自动)               │
│ • mint_tokens         │   │ • settle_event_market       │
│                       │   │   (创建者裁定)               │
│                       │   │ • redeem                    │
└───────────────────────┘   └──────────────┬──────────────┘
                                           │
                                           ▼
                                    ┌─────────────┐
                                    │ Pyth Oracle │
                                    │ (仅价格市场) │
                                    └─────────────┘

┌─────────────────────────────────────────────────────────┐
│                   Sega (Sonic SVM)                      │
│              处理 YES/NO Token 交易撮合                  │
└─────────────────────────────────────────────────────────┘
```

### 市场类型对比

| 类型 | 示例 | 投注代币 | 结算方式 |
|------|------|----------|----------|
| 价格预测 | "BTC 1/31 前到 $150k" | 创建者指定 | Pyth 自动 |
| 通用事件 | "Trump 1/31 前打伊朗?" | 创建者指定 | 创建者裁定 |

---

## Market Factory 合约

### Market 账户数据结构

```rust
pub struct Market {
    pub creator: Pubkey,           // 创建者（事件市场的裁定人）
    pub market_type: MarketType,   // Price / Event
    pub title: String,             // 市场标题
    pub description: String,       // 详细描述/规则

    // 投注代币
    pub bet_token_mint: Pubkey,    // 投注代币 CA（如 TRUMP）
    pub vault: Pubkey,             // 托管投注代币
    pub total_deposited: u64,      // 总存入数量

    // YES/NO 代币
    pub yes_mint: Pubkey,
    pub no_mint: Pubkey,

    // 时间和状态
    pub expiry_timestamp: i64,
    pub status: MarketStatus,      // Open / Settled
    pub outcome: Option<bool>,     // Some(true)=YES赢

    // 仅价格预测市场使用
    pub price_feed_id: Option<Pubkey>,
    pub target_price: Option<u64>,

    pub bump: u8,
}

pub enum MarketType {
    Price,  // Pyth 自动结算
    Event,  // 创建者裁定
}

pub enum MarketStatus {
    Open,
    Settled,
}
```

### 指令（Instructions）

```rust
// 创建价格预测市场
create_price_market {
    bet_token_mint: Pubkey,    // 投注代币 CA
    price_feed_id: Pubkey,     // Pyth 价格源
    target_price: u64,         // 目标价格
    expiry_timestamp: i64,     // 截止时间
}

// 创建通用事件市场
create_event_market {
    bet_token_mint: Pubkey,    // 投注代币 CA
    title: String,             // "Trump 1/31前打伊朗?"
    description: String,       // 详细规则描述
    expiry_timestamp: i64,     // 截止时间
}

// 铸造 YES/NO 代币（两种市场通用）
mint_tokens {
    market: Pubkey,
    amount: u64,               // 投注代币数量
}
// → 从用户转入指定代币到 Vault
// → 1:1 铸造等量 YES + NO 代币给用户
```

### 用户流程示例

```
1. 创建者发起市场：
   "Trump 1/31前打伊朗？"
   投注代币：TRUMP (CA: 6p6x...)
   截止时间：2026-01-31 00:00 UTC

2. 用户 A 存入 1000 TRUMP
   → 获得 1000 YES + 1000 NO

3. 用户 A 在 Sega 卖掉 NO，持有 YES（看多）

4. 截止后，创建者裁定：YES 赢

5. 用户 A 赎回 1000 YES → 获得相应 TRUMP
```

---

## Settlement 合约

### 指令（Instructions）

```rust
// 结算价格预测市场（任何人可触发）
settle_price_market {
    market: Pubkey,
}
// → 验证 market_type == Price
// → 验证已过期
// → 读取 Pyth 当前价格
// → 比较目标价，设置 outcome (true = 价格 >= 目标)
// → 更新 status 为 Settled

// 结算通用事件市场（仅创建者）
settle_event_market {
    market: Pubkey,
    outcome: bool,             // true = YES 赢, false = NO 赢
}
// → 验证 market_type == Event
// → 验证 caller == market.creator
// → 验证已过期
// → 设置 outcome
// → 更新 status 为 Settled

// 赎回代币（任何持有者）
redeem {
    market: Pubkey,
    amount: u64,               // 赎回的代币数量
}
// → 验证市场已结算
// → 销毁用户的获胜代币 (YES 或 NO)
// → 按比例从 Vault 转出投注代币
```

### 赎回计算逻辑

```
用户获得代币 = (用户获胜代币数量 / 获胜代币总供应量) × Vault 总额
```

举例：
- Vault 有 1000 TRUMP
- YES 赢了，YES 总供应量 800 枚
- 用户持有 80 枚 YES
- 用户获得：80 / 800 × 1000 = **100 TRUMP**

### Pyth 集成（仅价格市场）

```rust
use pyth_solana_receiver_sdk::price_update::PriceUpdateV2;

// 验证价格数据
let price_feed = &ctx.accounts.price_update;
let price = price_feed.get_price_no_older_than(
    &Clock::get()?,
    60,  // 最多60秒前的价格
    &market.price_feed_id.unwrap(),
)?;
```

### 安全检查

- 价格市场：任何人可触发结算，Pyth 自动判定
- 事件市场：只有创建者能裁定结果
- 市场必须已过期才能结算
- 赎回只接受获胜方代币
- 防止重复赎回（销毁机制）

### 风险提示

事件市场由创建者裁定，存在作恶风险。用户需信任创建者信誉。

可选的未来改进：
- 创建者需质押保证金
- 争议投票机制
- 信誉系统

---

## 前端架构

### 页面结构

```
/                     → 首页：市场列表（进行中 / 已结算）
/market/[id]          → 市场详情：信息、交易入口、结算状态
/create               → 创建市场：选择类型、填写参数
/portfolio            → 我的持仓 + 我创建的市场（可裁定）
```

### 创建市场页面

```
/create
├── 选择市场类型
│   ├── 价格预测 → 选币种、目标价、截止时间
│   └── 通用事件 → 输入标题、描述、截止时间
│
└── 通用字段
    └── 投注代币 CA（输入框，粘贴代币地址）
```

### 市场卡片展示

```
┌─────────────────────────────────────┐
│ 🎯 Trump 1/31前打伊朗?              │  ← 标题
│ 投注代币: TRUMP                      │  ← 显示代币名称
│ 创建者: 7xK2...                     │  ← 裁定人
│ 截止: 2026-01-31 00:00 UTC          │
│ 状态: 进行中                         │
│                                     │
│ [参与投注]  [去 Sega 交易]           │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 📈 BTC > $150,000                   │  ← 价格预测
│ 投注代币: USDC                       │
│ 数据源: Pyth (自动结算)              │  ← 标注自动
│ 截止: 2026-01-31 00:00 UTC          │
└─────────────────────────────────────┘
```

### 我的持仓页面

```
/portfolio
├── 我创建的市场（可裁定）
│   └── [裁定] 按钮（截止后出现）
│
└── 我参与的市场
    └── YES/NO 持仓 + [赎回] 按钮
```

### 核心组件

```
components/
├── MarketCard        → 市场卡片
├── PriceChart        → 实时价格图（Pyth 数据，仅价格市场）
├── TradePanel        → 交易面板（跳转 Sega）
├── CreateMarketForm  → 创建表单（支持两种类型）
├── SettleButton      → 裁定按钮（仅事件市场创建者）
├── RedeemButton      → 赎回按钮
└── WalletConnect     → 钱包连接
```

### 技术选型

| 功能 | 方案 |
|------|------|
| 框架 | Next.js 14 (App Router) |
| 样式 | Tailwind CSS |
| 钱包 | @solana/wallet-adapter-react |
| 合约交互 | @coral-xyz/anchor |
| 价格展示 | @pythnetwork/hermes-client |
| 状态管理 | React Query |

### 与 Sega 集成

交易按钮跳转到 Sega 页面进行 YES/NO 代币交易。

---

## 项目结构

```
sol-vibe-hack/
├── programs/
│   ├── market-factory/
│   │   └── src/lib.rs
│   │       ├── create_price_market
│   │       ├── create_event_market
│   │       └── mint_tokens
│   └── settlement/
│       └── src/lib.rs
│           ├── settle_price_market
│           ├── settle_event_market
│           └── redeem
├── app/
│   ├── page.tsx              # 首页（市场列表）
│   ├── market/[id]/page.tsx  # 市场详情
│   ├── create/page.tsx       # 创建市场
│   ├── portfolio/page.tsx    # 我的持仓 + 裁定
│   └── components/
├── tests/
├── Anchor.toml
├── package.json
└── README.md
```

---

## 配置参考

### Sonic SVM 配置 (Anchor.toml)

```toml
[provider]
cluster = "https://api.testnet.sonic.game"
wallet = "~/.config/solana/id.json"

[programs.localnet]
market_factory = "..."
settlement = "..."
```

### Pyth Price Feed IDs (Sonic SVM)

- BTC/USD: `0xe62df6c8b4a85fe1a67db44dc12de5db330f7ac66b72dc658afedf0f4a415b43`
- ETH/USD: `0xff61491a931112ddf1bd8147cd1b641375f79f5825126d665480874634fd0ace`
- SOL/USD: `0xef0d8b6fda2ceba41da15d4095d1da392a0d2f8ed0c6c7bc0f4cfac8c280b56d`

---

## 功能清单

| 功能 | 描述 |
|------|------|
| 创建价格预测市场 | 选币种 + 目标价 + 投注代币 + 截止时间 |
| 创建通用事件市场 | 标题 + 描述 + 投注代币 + 截止时间 |
| 铸造代币 | 存入投注代币，1:1 获得 YES + NO |
| Sega 交易 | 跳转 Sega 买卖 YES/NO 代币 |
| 价格市场结算 | Pyth 自动判定 |
| 事件市场裁定 | 创建者判定结果 |
| 赎回 | 销毁获胜代币，取回投注代币 |
